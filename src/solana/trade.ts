export async function buyToken(tokenAddress: string, amount: number) {

  console.log(`Buying ${amount} of ${tokenAddress}`);

  // swap logic placeholder

  return {
    success: true
  };
}

export async function sellToken(tokenAddress: string, amount: number) {

  console.log(`Selling ${amount} of ${tokenAddress}`);

  // swap logic placeholder

  return {
    success: true
  };
}
